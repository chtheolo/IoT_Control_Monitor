{
  "name": "auth",
  "version": "1.0.0",
  "description": "This is a service for authorizing the user requests that wants to use the rest of the other IoT services",
  "main": "index.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "ts-node-dev --respawn --pretty --transpile-only index.ts",
    "docker": "sudo docker-compose up --build"
  },
  "author": "Chris Theologou",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^10.0.0",
    "express": "^4.17.1",
    "http": "0.0.1-security",
    "ts-node-dev": "^1.1.8"
  },
  "devDependencies": {
    "@types/cors": "^2.8.12",
    "@types/dotenv": "^8.2.0",
    "@types/express": "^4.17.13",
    "@types/node": "^16.10.2",
    "typescript": "^4.4.3"
  }
}
